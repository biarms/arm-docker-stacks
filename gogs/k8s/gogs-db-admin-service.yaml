apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: gogs-db-admin
  name: gogs-db-admin
spec:
  clusterIP: None
  # type: NodePort
  ports:
  - name: pgadmin
    port: 5050
    targetPort: 5050
    # nodePort: 30550
  selector:
    io.kompose.service: gogs-db-admin
status:
  loadBalancer: {}
